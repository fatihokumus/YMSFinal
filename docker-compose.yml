version: '3'

services:
  glassfish:
    build: .
    container_name: glassfish
    ports:
      - "8081:8080"

  nginx:
    image: nginx:latest
    container_name: nginx
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - glassfish